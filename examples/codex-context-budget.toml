# Merge these values into ~/.codex/config.toml for GPT-5.6 Codex CLI runs.
#
# The 272K cap targets the reported long-context pricing boundary. Compact at
# 240K to leave 32K tokens of headroom for the current turn, tool results,
# reasoning, and output. This is separate from LiteLLM model_info accounting.
model_context_window = 272000
model_auto_compact_token_limit = 240000
